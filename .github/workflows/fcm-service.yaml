name: Build and Deploy FCM Service

on:
  push:
    branches:
      - main
    paths:
      - 'fcm-service/**'

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      # 1. Checkout repo
      - name: Checkout code
        uses: actions/checkout@v4

      # 2. Log in to Docker Hub
      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      # 3. Build Docker image for API Gateway
      - name: Build Docker image
        run: docker build -t joelgallan/fcm-service:latest ./fcm-service

      # 4. Push Docker image
      - name: Push Docker image
        run: docker push joelgallan/fcm-service:latest

      # 5. Deploy API Gateway on VPS
      - name: Deploy to VPS
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USER }}
          key: ${{ secrets.VPS_SSH_KEY }}
          script: |
            cd ~/apps/circle-app
            docker compose stop fcm-service
            docker compose rm -f fcm-service
            docker compose pull fcm-service
            docker compose up -d fcm-service
            docker image prune -f || echo "Prune skipped"
